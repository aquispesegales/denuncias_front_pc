import types from '@/store/types/casos'
import Vue from 'vue';

const state = {
 
}
const getters = {
 
}
const mutations = {
  
}
const actions = {
  
}
export default {
  state,
  actions,
  getters,
  mutations
}